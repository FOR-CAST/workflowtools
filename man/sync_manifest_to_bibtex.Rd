% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_bibtex.R
\name{sync_manifest_to_bibtex}
\alias{sync_manifest_to_bibtex}
\title{Sync manifest citations to a sidecar BibTeX file}
\usage{
sync_manifest_to_bibtex(
  manifest = "data/input_manifest.json",
  out = "citations/data-sources.bib",
  references_bib = "citations/references.bib",
  prune = FALSE
)
}
\arguments{
\item{manifest}{Path to the manifest JSON file.}

\item{out}{Path to the sidecar .bib file to write. Created if missing.}

\item{references_bib}{Path to the curated bibliography. Read for key
deduplication only; never modified.}

\item{prune}{Logical. When \code{TRUE}, removes any entries already in \code{out}
whose keys are no longer in the manifest. When \code{FALSE} (default), the
function fully regenerates \code{out} from the manifest each call, which
has the same effect; the argument is reserved for a future
incremental-merge mode.}
}
\value{
A list summarising the write: \code{written} (integer count),
\code{skipped_external} (integer count), \code{skipped_collision} (character
vector of keys that collided with \code{references_bib}), invisibly.
}
\description{
One-way, conservative writer. Reads the manifest and the existing
(curated) references bib; for every manifest record whose citation is
eligible, writes one BibTeX entry to \code{out}. Eligible means:
}
\details{
\itemize{
\item The record has a \code{citation} field with a \code{bibtex_key}.
\item \code{citation$external} is not \code{TRUE} (records flagged \code{external = TRUE}
live in the curated bib and are deliberately not re-exported).
\item The \code{bibtex_key} is not already present in \code{references_bib} (warns and
skips on collision so the curated bib stays the source of truth for
keys it owns).
}

Records are sorted by \code{bibtex_key} for stable diffs. The output file
starts with a banner identifying it as auto-generated; do not edit it
by hand.
}
