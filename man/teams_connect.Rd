% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{teams_connect}
\alias{teams_connect}
\title{Connect to a Microsoft Teams channel's file area}
\usage{
teams_connect(
  team,
  channel,
  tenant,
  app = "04b07795-8ddb-461a-bbee-02f9e1bf7b46",
  auth_type = "device_code"
)
}
\arguments{
\item{team}{Team name or id (passed to \code{\link[Microsoft365R:get_team]{Microsoft365R::get_team()}}).}

\item{channel}{Channel name (e.g. \code{"General"}).}

\item{tenant}{Azure tenant (e.g. an organisation short name).}

\item{app}{Azure app (client) id. Defaults to the well-known Azure CLI
public-client id, which sidesteps needing a tenant admin to approve a
bespoke app registration (see the \pkg{Microsoft365R} auth vignette).}

\item{auth_type}{Authentication flow; defaults to \code{"device_code"}.}
}
\value{
A named list with elements \code{team} (an \code{ms_team}), \code{channel} (an
\code{ms_channel}), and \code{folder} (the channel root \code{ms_drive_item}).
}
\description{
Authenticates against Microsoft Graph and resolves a team, one of its
channels, and that channel's root drive folder. \pkg{Microsoft365R} (via
\pkg{AzureAuth}) caches the token under the AzureR cache dir, so after a
successful device-code login subsequent calls reuse the cached token.
}
