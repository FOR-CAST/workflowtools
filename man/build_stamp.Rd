% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_stamp.R
\name{build_stamp_git}
\alias{build_stamp_git}
\alias{build_stamp_date}
\title{Build stamp for report title blocks}
\usage{
build_stamp_git(repo = ".")

build_stamp_date(date = Sys.Date(), repo = ".")
}
\arguments{
\item{repo}{Path to the git repository. Defaults to the current working
directory (\code{"."}), since the Quarto render subprocess runs with the
project root as its working directory.}

\item{date}{A \code{Date} (or date-like value accepted by \code{format()}) to display.
Defaults to \code{Sys.Date()}.}
}
\value{
\itemize{
\item \code{build_stamp_git()}: a list with \code{sha} (character), \code{branch} (character),
and \code{dirty} (logical).
\item \code{build_stamp_date()}: a length-1 character string.
}
}
\description{
Compact git/date build-stamp helpers for embedding a traceable identifier
in a rendered document's title block (e.g. a Quarto \verb{date:} field). Any two
PDFs carrying the same stamp can be traced back to the exact code that
produced them.
}
\details{
Kept dependency-free (base R plus \code{git} on \code{PATH} only) so they can be
sourced from a Quarto \verb{date:} inline expression without pulling in heavier
packages.
\itemize{
\item \code{build_stamp_git()} returns raw git facts (short SHA, branch, dirty flag).
Each shell-out is wrapped so a missing \code{git} (or a non-repo checkout)
degrades to \code{"unknown"}/clean rather than erroring.
\item \code{build_stamp_date()} formats a one-line stamp for a \verb{date:} field.
ASCII-only and free of em-dashes so it renders cleanly under both
pdflatex and xelatex. Examples:
clean tree: \code{"2026-06-03 (commit a1b2c3d)"};
dirty tree: \code{"2026-06-03 (commit a1b2c3d, uncommitted changes)"}.
}
}
\seealso{
Other provenance:
\code{\link[=prov_build_identity]{prov_build_identity()}}
}
\concept{provenance}
