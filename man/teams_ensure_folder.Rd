% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{teams_ensure_folder}
\alias{teams_ensure_folder}
\title{Ensure a (possibly nested) Teams folder exists}
\usage{
teams_ensure_folder(folder, path)
}
\arguments{
\item{folder}{The channel root \code{ms_drive_item} (from \code{\link[=teams_connect]{teams_connect()}}).}

\item{path}{Folder path relative to \code{folder}, "/" separated.}
}
\value{
The \code{ms_drive_item} for the final (deepest) folder in \code{path}.
}
\description{
Creates each missing level of \code{path} by name against its resolved parent item
(\code{parent$create_folder(name)}) rather than passing a multi-segment path to
\code{create_folder()}, so we never rely on the Graph API auto-creating
intermediate folders. This matters when the whole path is new.
}
