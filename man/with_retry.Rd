% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{with_retry}
\alias{with_retry}
\title{Retry a flaky network call with a hardened curl config}
\usage{
with_retry(fn, times = 3L, wait = 3, http11 = TRUE)
}
\arguments{
\item{fn}{A zero-argument function performing the network call.}

\item{times}{Integer; maximum number of attempts.}

\item{wait}{Numeric; seconds to sleep between attempts.}

\item{http11}{Logical; force HTTP/1.1 for the call.}
}
\value{
The value of \code{fn()} on the first successful attempt. The last error is
re-raised if every attempt fails.
}
\description{
Wraps a network-facing call so a transient failure (token refresh, throttling,
dropped socket, HTTP/2 framing error) is retried with a short backoff rather
than aborting a long recursive walk or multi-file transfer. When \code{http11} is
\code{TRUE} (the default) it also forces HTTP/1.1 for the duration of the attempts
(the curl "HTTP2 framing layer" workaround) via an internal helper.
}
