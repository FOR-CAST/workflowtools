% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{archive_extract_once}
\alias{archive_extract_once}
\title{Extract an archive, skipping if previously extracted}
\usage{
archive_extract_once(archive, dir = ".", files = NULL, ..., force = FALSE)
}
\arguments{
\item{archive}{\code{character(1)} The archive filename or an \code{archive} object.}

\item{dir}{\code{character(1)} Directory location to extract archive contents, will be created
if it does not exist.}

\item{files}{\code{character() || integer() || NULL} One or more files within the archive,
specified either by filename or by position.}

\item{...}{additional parameters passed to \link[archive:archive_extract]{archive::archive_extract}}

\item{force}{logical, whether to extract and overwrite existing files}
}
\value{
character, the filenames extracted (invisibly)
}
\description{
Extract an archive, skipping if previously extracted
}
\details{
Extraction is skipped only when every wanted file is already on disk \strong{at the size
the archive says it should be}. Checking existence alone is not enough: an
interrupted extraction leaves a short file behind, which then looks "already
extracted" forever.

Extraction goes into a staging directory (a child of \code{dir}, removed on exit) and is
verified against the archive manifest there; only then are the members moved into
\code{dir}. An error is raised if anything is still missing or short, so a partial
extraction fails loudly instead of being silently reused -- and, because nothing is
moved until the whole set verifies, a failure part-way through can no longer leave a
truncated file in \code{dir}. That matters because a single short member makes the next
call re-extract the \emph{whole} archive, so extracting in place turns one interrupted run
into a permanent loop. Note this needs room for a second copy of the extracted
members while the staging directory exists.

\code{libarchive} (via \link[archive:archive_extract]{archive::archive_extract}) can fail on very large \code{ZIP64}
archives (e.g. with a "Truncated input file" error). When extraction fails and
the archive is a \code{.zip}, \code{archive_extract_once()} falls back to the system
\code{unzip} (Info-ZIP), which handles \code{ZIP64} and selective extraction.
}
\examples{
a <- system.file(package = "archive", "extdata", "data.zip")
d <- tempfile()
f <- c("iris.csv", "airquality.csv")
i <- c(1L, 3L)

archive_extract_once(a, d)
list.files(d, full.names = TRUE)
archive_extract_once(a, d) ## does not re-extract files
unlink(d)

d <- tempfile()
archive_extract_once(a, d, f)
list.files(d, full.names = TRUE)
archive_extract_once(a, d, f) ## does not re-extract files
unlink(d)

d <- tempfile()
archive_extract_once(a, d, i)
list.files(d, full.names = TRUE)
archive_extract_once(a, d, i) ## does not re-extract files
unlink(d)

}
