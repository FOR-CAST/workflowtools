% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{teams_upload_files}
\alias{teams_upload_files}
\title{Upload local files into a Teams folder}
\usage{
teams_upload_files(folder, files, retries = 3L)
}
\arguments{
\item{folder}{The destination \code{ms_drive_item}.}

\item{files}{Character vector of local file paths to upload.}

\item{retries}{Integer; per-call retry count passed to \code{\link[=with_retry]{with_retry()}}.}
}
\value{
A data frame manifest with columns \code{file} (basename) and
\code{uploaded_at} (a timestamp string), one row per uploaded file.
}
\description{
Uploads each existing file in \code{files} into the destination \code{folder} (an
\code{ms_drive_item}, e.g. from \code{\link[=teams_ensure_folder]{teams_ensure_folder()}}). Missing files are
silently skipped. An upload replaces any existing item of the same name. Each
upload is wrapped in \code{\link[=with_retry]{with_retry()}}.
}
