% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{teams_download_folder}
\alias{teams_download_folder}
\title{Recursively mirror a Teams folder to a local directory}
\usage{
teams_download_folder(
  folder,
  path,
  remote_path = "",
  exclude = character(0),
  overwrite = TRUE,
  retries = 3L
)
}
\arguments{
\item{folder}{The channel root \code{ms_drive_item} (from \code{\link[=teams_connect]{teams_connect()}}).}

\item{path}{Local directory to mirror into.}

\item{remote_path}{Remote path relative to \code{folder}, "/" separated; \code{""}
(the default) means the folder root.}

\item{exclude}{Character vector of remote paths (relative to \code{folder}, "/"
separated) to skip entirely; matching files or whole subtrees are not
descended into or downloaded.}

\item{overwrite}{Logical; when \code{FALSE}, existing local files are kept
(resumable across flaky auth).}

\item{retries}{Integer; per-call retry count passed to \code{\link[=with_retry]{with_retry()}}.}
}
\value{
Invisibly, a character vector of the local paths downloaded/updated.
}
\description{
Walks the remote tree itself (list items, recreate directories, download
files one at a time, each wrapped in \code{\link[=with_retry]{with_retry()}}). This is more reliable
than the built-in \code{ms_drive_item$download(recursive = TRUE)}, which has been
observed to silently skip nested subfolders.
}
